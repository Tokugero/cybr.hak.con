# .abstract.md — rag-mcp-build

L0 project map. For full detail, read `.overview.md` (L1) or each subsystem's own `.abstract.md` and `.overview.md`.

Personal security research assistant: ingest the HackTricks GitHub repository once, expose a vector-search endpoint via MCP so an LLM agent can look up tools and techniques on demand without re-reading every page.

| Subsystem | Path | Purpose |
|-----------|------|---------|
| library | `lib/` | Ingestion, vector storage, and search logic |
| mcp | `mcp/` | MCP server wrapping the library; loaded locally by the LLM harness |
