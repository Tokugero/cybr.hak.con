# .abstract.md — mcp

L0 subsystem map. For full detail, read `.overview.md` (L1).

| File | Purpose |
|------|---------|
| `src/mcp_server/__init__.py` | Public re-exports (`mcp`) |
| `src/mcp_server/__main__.py` | Module entrypoint — calls `main()` for `python -m mcp_server` |
| `src/mcp_server/server.py` | FastMCP app, search tool, `main()` wiring |
| `src/mcp_server/schemas.py` | Pydantic DTOs: `SearchResultDTO`, `SearchResponse` |
